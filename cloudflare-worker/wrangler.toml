# Despliegue por línea de comandos:
#   npm install -g wrangler
#   wrangler login
#   wrangler deploy
#
# Al terminar imprime la dirección del Worker. Pégala en MarketSurge Web:
# Ajustes ⚙ → "Tu propio proxy".

name = "yahoo-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Para aceptar solo peticiones desde tu web, descomenta y ajusta:
# [vars]
# ALLOWED_ORIGINS = "https://ggallo100.github.io"
